;;; blood-bind--layer.el -*- lexical-binding: t; no-byte-compile: t; -*-

;; Layer : a simplified evil state

(defun blood-bind-define-layer (name)
  "Define a layer, a *non-exclusive* collection of keymaps"
  )

(defun blood-bind-define-state (name)
  "Define a state, an *exclusive* collection of keymaps"

  )


(provide 'blood-bind--layer)
;;; blood-bind--layer.el ends here
